// next.config.js
/**
 * Next.js configuration file.
 *
 * The project previously used a `next.config.ts` file, which is not supported by
 * the current version of Next.js. Replacing it with a JavaScript configuration
 * file restores compatibility with the Next.js build process.
 */

/** @type {import('next').NextConfig} */
const nextConfig = {
  // Add any custom Next.js configuration here. For now we keep it empty
  // because the project does not require special settings.
};

module.exports = nextConfig;
